import Typography from "@mui/material/Typography";

const Tasks = () => {
    return (
        <div>
            <Typography
                variant="h5"
                sx={{
                    textAlign: "center",
                    marginTop: 5,
                }}
            >
                Tasks
            </Typography>
        </div>
    );
};

export default Tasks;
