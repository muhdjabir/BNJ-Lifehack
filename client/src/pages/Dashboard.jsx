import Typography from "@mui/material/Typography";

const Dashboard = () => {
    return (
        <div>
            <Typography
                variant="h5"
                sx={{
                    textAlign: "center",
                    marginTop: 5,
                }}
            >
                Dashboard
            </Typography>
        </div>
    );
};

export default Dashboard;
